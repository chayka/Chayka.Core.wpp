build_settings:
#    clone_depth: 1 # depth of 1 is a shallow clone, remove this line to clone entire repo

setup:
    composer:
        action: "install"
    copy_build:
        directory: "${WP_CI_DIR}wp-content/plugins/Chayka.Core.wpp"

test:
    shell:
        - "phpunit --tap"
        - "bash ./tests/casperjs/test.sh"
#    php_unit:
#        config:
#            - "phpunit.xml.dist"
#        directory:
#            - "tests/"
#        run_from: "phpunit/"
#        coverage: "tests/logs/coverage"

#    shell:

complete:
